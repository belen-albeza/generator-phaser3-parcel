# <%= title %>

## Build

Install dependencies:

```sh
npm install
```

Build for development:

```sh
npm start
```

Build for production (files will be copied to a `dist` folder):

```sh
npm run build
```
